# Optimizer change requests

Write exactly what you want changed below. Then run the GitHub Action:
Actions → Optimizer Bot → Run workflow.

Example:
- Update VocabDeck prompt tone to “BRO TERM” style
